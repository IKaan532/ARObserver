services:
  arobserver:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./targets.yaml:/app/targets.yaml:ro
    restart: unless-stopped
